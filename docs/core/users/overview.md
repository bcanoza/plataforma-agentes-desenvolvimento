# Users – Overview

- `GET /v1/users/me`: Perfil completo para montar UI.
- `PUT /v1/users/me/preferences`: Atualiza `themePreferred`, `moduleOrder`, `iconOrder`.
- Admin: CRUD de usuários, habilitar/desabilitar, reset de senha.
